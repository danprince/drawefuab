{
  "name": "drawefuab-multiplayer",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "cd dist && bun server.js",
    "build": "bun build --target bun --production --outdir=dist ./server.tsx",
    "dev": "bun --watch server.tsx"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "@fontsource/schoolbell": "^5.2.7",
    "@kitajs/html": "^4.2.13",
    "morphlex": "^1.4.0"
  }
}
